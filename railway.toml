# Railway will auto-detect Node.js and run "npm start"
# No extra config needed — Railway reads package.json automatically

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
